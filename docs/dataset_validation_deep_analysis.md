# Глубокий анализ проблемы валидации датасета

## Резюме проблемы

**Проблема**: Все 34 фичи с `lookback_window: '15m'` имеют 88.4% NaN значений, начиная с `2025-12-20T16:40:00+00:00`. Валидация полноты фич проходит успешно, но валидация качества данных падает.

**Ключевые факты**:
- Валидация полноты: ✅ Все фичи присутствуют, есть хотя бы одно не-NaN значение
- Валидация качества: ❌ 88.4% NaN для всех lookback фич
- Первое пропущенное значение: `2025-12-20T16:40:00+00:00`
- Последнее пропущенное значение: `2025-12-25T23:59:00+00:00`
- Общее количество пропущенных строк: 7640 из 8640 (88.4%)

## Детальный анализ корневой причины

### 1. Проблема с агрегацией 1m -> 5m klines

**Функция**: `compute_all_candle_patterns_15m` в `candle_patterns.py`

**Проблема**:
1. Функция пытается получить 5m klines из `RollingWindows`, но их нет (всегда 0)
2. Происходит fallback на 1m klines с агрегацией в 5m
3. Для агрегации требуется минимум **15 минут истории** (3 свечи × 5 минут)
4. Если данных недостаточно, функция возвращает `_get_empty_features_dict()` - все фичи = `None`

**Логи подтверждают**:
```
compute_all_candle_patterns_15m_no_5m_klines
klines_5m_count=0
falling_back_to_1m=True
```

**Код проблемы** (строки 736-789 в `candle_patterns.py`):
```python
# Try to get 5m candles first, but fall back to 1m if not available
klines = rolling_windows.get_klines_for_window("5m", start_time, end_time)

if len(klines) < 3:
    # Fall back to 1m candles
    klines_1m = rolling_windows.get_klines_for_window("1m", start_time, end_time)
    
    if len(klines_sorted) < 5:
        return _get_empty_features_dict()  # ❌ ВСЕ ФИЧИ = None
```

### 2. Проблема с временной границей данных

**Граничный timestamp**: `2025-12-20T16:40:00+00:00`

**Анализ**:
- Для timestamp `16:40:00` требуется история до `16:25:00` (15 минут назад)
- Если данные начинаются с `16:40:00`, то для первых 15 минут нет истории
- Это приводит к тому, что `compute_all_candle_patterns_15m` возвращает пустые фичи
- После накопления 15 минут истории проблема должна исчезнуть

**НО**: Проблема продолжается до `2025-12-25T23:59:00`, что указывает на системную проблему, а не только на граничные условия.

### 3. Проблема в HybridFeatureComputer

**Функция**: `compute_all_candle_patterns` в `hybrid_feature_computer.py`

**Проблема**:
1. Функция вызывает `compute_all_candle_patterns_15m` для каждого timestamp
2. Если функция возвращает `_get_empty_features_dict()` (все `None`), это не обнаруживается
3. Значения `None` присваиваются в DataFrame, что превращается в NaN

**Код проблемы** (строки 698-707):
```python
patterns = compute_patterns(rolling_windows)

# Check if patterns is empty (all None) - но проверка недостаточна
if not patterns or all(v is None for v in patterns.values()):
    # ❌ Но это не обрабатывается правильно
    pass

# Assign values to DataFrame
for feature_name, value in patterns.items():
    result.at[idx, feature_name] = value  # value = None -> становится NaN
```

### 4. Проблема в валидации

**Валидация полноты** (`_validate_feature_completeness`):
- Проверяет: `feature_series.isna().all()` - если False, то есть хотя бы одно не-NaN
- ✅ Проходит, потому что есть хотя бы 1000 не-NaN значений (первые 1000 строк до 16:40)

**Валидация качества** (`_validate_data_quality`):
- Проверяет: `nan_ratio > threshold` для каждой фичи
- ❌ Падает, потому что `nan_ratio = 0.884` (7640 NaN из 8640)

**Почему не было обнаружено раньше**:
- Первые 1000 строк имеют данные (до 16:40)
- Валидация полноты проверяет только наличие хотя бы одного не-NaN
- Валидация качества проверяет процент NaN, но слишком поздно

## Цепочка событий

1. **Расчет датасета начинается с `2025-12-20T00:00:00`**
2. **Первые 1000 строк (до 16:40)**: Данные есть, фичи рассчитываются успешно
3. **С 16:40:00**: Для каждого timestamp требуется 15 минут истории назад
4. **Проблема**: Данные начинаются с 00:00:00, но для агрегации 1m->5m требуется минимум 15 минут контекста
5. **Результат**: `compute_all_candle_patterns_15m` возвращает пустые фичи для всех строк после 16:40
6. **Валидация полноты**: Видит, что есть хотя бы 1000 не-NaN значений → проходит
7. **Валидация качества**: Видит, что 88.4% NaN → падает

## Технические детали

### Почему нет 5m klines?

**Причина**: `RollingWindows` не хранит агрегированные 5m klines, только 1m. Агрегация происходит на лету в `compute_all_candle_patterns_15m`.

### Почему агрегация требует 15 минут?

**Требования**:
- Для создания одной 5m свечи нужно минимум 5 минут данных
- Для 3 свечей (паттерны) нужно минимум 15 минут
- Для надежности добавляется буфер +1 минута → требуется 16 минут истории

### Почему проблема продолжается до конца периода?

**Гипотеза**: 
- Либо данные действительно отсутствуют после 16:40
- Либо есть проблема с логикой проверки доступности данных
- Либо проблема в самой агрегации 1m->5m (например, неправильная группировка по time_bucket)

## Решения

### Решение 1: Улучшить обработку недостаточных данных

**В `compute_all_candle_patterns_15m`**:
- Вместо возврата пустых фичей для недостаточных данных, использовать аппроксимацию
- Использовать последние доступные данные для заполнения недостающих свечей
- Добавить детальное логирование недостающих данных

### Решение 2: Исправить проверку доступности данных

**В `HybridFeatureComputer`**:
- Улучшить проверку доступности данных перед вызовом `compute_all_candle_patterns_15m`
- Проверять не только наличие klines, но и их достаточность для агрегации
- Предотвращать вызов функции, если данных недостаточно

### Решение 3: Хранить 5m klines в RollingWindows

**Архитектурное решение**:
- Добавить хранение агрегированных 5m klines в `RollingWindows`
- Обновлять их автоматически при добавлении новых 1m klines
- Это улучшит производительность и надежность

### Решение 4: Улучшить валидацию

**В `_validate_data_quality`**:
- Добавить проверку временных паттернов пропусков (если пропуски идут подряд, это проблема данных, а не случайные)
- Улучшить диагностику: показывать не только процент NaN, но и временные интервалы пропусков
- Добавить предупреждение, если пропуски начинаются с определенного timestamp

## Рекомендации по исправлению

### Приоритет 1: Критический (немедленное исправление)

1. **Исправить обработку недостаточных данных в `compute_all_candle_patterns_15m`**:
   - Использовать аппроксимацию вместо пустых фичей
   - Добавить детальное логирование

2. **Улучшить проверку доступности данных**:
   - Проверять достаточность данных перед вызовом функции
   - Предотвращать возврат пустых фичей

### Приоритет 2: Важный (в ближайшее время)

3. **Улучшить валидацию**:
   - Добавить проверку временных паттернов пропусков
   - Улучшить диагностику

4. **Добавить мониторинг**:
   - Отслеживать случаи, когда агрегация возвращает пустые фичи
   - Алертинг на высокий процент пропусков

### Приоритет 3: Улучшения (долгосрочно)

5. **Архитектурные улучшения**:
   - Хранить агрегированные klines в RollingWindows
   - Кэшировать результаты агрегации

## КРИТИЧЕСКАЯ НАХОДКА: Проблема с фильтрацией окна данных 🔴

### Обнаруженная проблема

**Проблема в `OptimizedRollingWindow.get_window()`**:

Для каждого timestamp `get_window()` фильтрует данные:
```python
cutoff_time = end_timestamp - self.max_lookback  # max_lookback = 30 + 5 = 35 минут
klines_window = self.klines_buffer[
    (self.klines_buffer["timestamp"] >= cutoff_time) &
    (self.klines_buffer["timestamp"] <= end_timestamp)
]
```

**НО!** Функция `compute_all_candle_patterns_15m` требует другое окно:
```python
start_time = end_time - timedelta(minutes=16)  # 15 минут + 1 минута буфер
klines_1m = rolling_windows.get_klines_for_window("1m", start_time, end_time)
```

### Пример для timestamp `16:40:00`:

1. **`get_window(16:40:00)`** создает `RollingWindows` с данными:
   - `cutoff_time = 16:40:00 - 35 минут = 16:05:00`
   - Данные в окне: `[16:05:00, 16:40:00]` ✅

2. **`compute_all_candle_patterns_15m`** запрашивает данные через `get_klines_for_window`:
   - `start_time = 16:40:00 - 16 минут = 16:24:00`
   - `end_time = 16:40:00`
   - Запрос данных: `[16:24:00, 16:40:00]` ✅

3. **НО ПРОБЛЕМА**: `get_klines_for_window` вызывается на `RollingWindows`, которое уже отфильтровано методом `get_window()`:
   - В `RollingWindows` уже есть только данные `[16:05:00, 16:40:00]`
   - `get_klines_for_window("1m", 16:24:00, 16:40:00)` должна работать правильно ✅

### НО! Проблема может быть в другом:

**Гипотеза**: Проблема в том, что `max_lookback_minutes` вычисляется как `requirements.max_lookback_minutes`, который может быть **30 минут**, но `compute_all_candle_patterns_15m` требует **15 минут** для агрегации. 

**Проверка**: Если `max_lookback_minutes = 15` (а не 30), то:
- `cutoff_time = 16:40:00 - 20 минут (15+5 буфер) = 16:20:00`
- Но для агрегации нужно `16:24:00`!
- **Результат**: В окне нет данных до `16:24:00` → агрегация не работает!

### Дополнительная проблема: граница периода

Для timestamp `16:40:00` первого дня (`2025-12-20`):
- `data_start = 2025-12-20 00:00:00 - (max_lookback + buffer) = 2025-12-19 23:10:00` (если max_lookback=30, buffer=20)
- Но если данные в базе начинаются только с `2025-12-20 00:00:00`, то для `16:40:00`:
  - Нужна история до `16:25:00` (для агрегации)
  - Данные доступны начиная с `00:00:00` ✅
  - **НО**: Если `cutoff_time` выходит за пределы загруженных данных, проблема остается

### Вывод: КРИТИЧЕСКАЯ ПРОБЛЕМА НАЙДЕНА! 🔴

**Проблема**: Для timestamp'ов первого дня периода (`2025-12-20`) данные в базе начинаются только с `00:00:00`, но для timestamp `16:40:00` требуется история на 35 минут назад (до `16:05:00`). 

**НО**: Данные в базе заполнены полностью (24 часа), но **`get_window()` фильтрует данные относительно `end_timestamp`**, и если данные начинаются только с `00:00:00`, то:
- Для timestamp `16:40:00`: `cutoff_time = 16:05:00` ✅
- Данные в буфере: `[00:00:00, ...]` ✅
- Данные после фильтрации: `[16:05:00, 16:40:00]` ✅
- Для агрегации нужно: `[16:24:00, 16:40:00]` ✅

**НО ПРОБЛЕМА МОЖЕТ БЫТЬ В ДРУГОМ**:

**Гипотеза 1**: Данные загружаются с `data_start = 2025-12-19T23:10:00`, но если в базе данных нет за `2025-12-19`, то буфер остается пустым или неполным.

**Гипотеза 2**: В `OptimizedRollingWindow.get_window()` окно фильтруется правильно, НО проблема в том, что для **первых timestamp'ов периода** (например, `00:00:00`) `cutoff_time` выходит за пределы загруженных данных:
- Для `16:40:00`: `cutoff_time = 16:05:00` ✅
- Но если данные начинаются только с `00:00:00`, то для ранних timestamp'ов (например, `00:15:00`) `cutoff_time = 2025-12-19 23:40:00` ❌ - данных нет!

**Проверка из логов**:
- `data_start = 2025-12-19T23:10:00` ✅ - данные должны загружаться с 19 декабря
- `klines_count = 8682` ✅ - данные загружены
- Проблема начинается с `2025-12-20T16:40:00`, а не с начала дня

**ВЫВОД**: Данные загружаются правильно, проблема может быть в другом:

**Возможная причина**: В `compute_all_candle_patterns_15m` при агрегации 1m->5m используется неправильная группировка по `time_bucket`, которая может неправильно обрабатывать timestamp'ы на границе дней или часов.

**Или**: Проблема в том, что для timestamp `16:40:00` требуется минимум 5 минут данных для создания одной 5m свечи, но при группировке по `time_bucket` (основанной на `timestamp.min()`) может получаться неправильная группировка, если данные не начинаются точно с границы 5-минутного интервала.

**КРИТИЧЕСКИЙ МОМЕНТ**: Нужно проверить, правильно ли работает агрегация 1m->5m в `compute_all_candle_patterns_15m` - возможно, группировка по `time_bucket` создает неправильные 5m свечи.

## Заключение

**Корневая причина**: Проблема с фильтрацией данных в `OptimizedRollingWindow.get_window()` - окно может быть обрезано слишком рано (`cutoff_time`), не оставляя достаточно данных для агрегации в `compute_all_candle_patterns_15m`. 

**Возможные причины**:
1. `max_lookback_minutes` слишком мал (15 вместо 30)
2. `cutoff_time` вычисляется неправильно
3. Данные не загружаются полностью в буфер (граница периода)

**Исправление требует**:
1. Проверить и исправить вычисление `max_lookback_minutes` для версии 1.5.0
2. Убедиться, что `cutoff_time` в `get_window()` учитывает требования агрегации (минимум 16 минут для 15m паттернов)
3. Добавить проверку достаточности данных перед вызовом `compute_all_candle_patterns_15m`
4. Улучшить логирование границ окон для диагностики

