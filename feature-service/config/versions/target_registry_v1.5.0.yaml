version: 1.5.0
config:
  type: regression
  horizon: 1200
  threshold: null
  computation:
    preset: volatility_normalized_std
    overrides:
      price_source: close
      future_price_source: close
      lookup_method: nearest_forward
      tolerance_seconds: null
    options:
      volatility_window: 12
description: 'Target Registry version 1.5.0 - volatility-normalized returns using
  rolling_std. Formula: raw_return / rolling_std(raw_return, window=20). Provides
  stable target across different market volatility regimes.'
